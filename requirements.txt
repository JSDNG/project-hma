# --- FastAPI service ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.5
requests>=2.31.0

# --- Other automation deps in this repo (optional for the API itself) ---
playwright>=1.58.0
undetected-playwright>=0.3.0

# --- Tests ---
pytest>=8.0
httpx>=0.27

# Setup (macOS / Linux):
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   # only if you use the playwright deps:
#   python -m playwright install chromium
#
# Setup (Windows PowerShell):
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# Run the FastAPI service (any OS, venv active):
#   uvicorn app.main:app --reload
